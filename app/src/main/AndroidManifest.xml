<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android_tv_show_notifier" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/top_tv"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:hardwareAccelerated="false"
        android:theme="@style/Theme.Androidtvshownotifier" >
        <activity
            android:name=".activities.FavouriteActivity"
            android:exported="false"
            android:theme="@style/Theme.Androidtvshownotifier.NoActionBar" />
        <activity
            android:name=".activities.TitleActivity"
            android:exported="false"
            android:theme="@style/Theme.Androidtvshownotifier.NoActionBar" />
        <activity
            android:name=".activities.ActorActivity"
            android:exported="false"
            android:theme="@style/Theme.Androidtvshownotifier.NoActionBar" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Androidtvshownotifier.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>